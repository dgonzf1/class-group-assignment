use crate::structs::class::Class;
use crate::structs::student::Student;
use std::collections::HashMap;

pub fn assign_class_and_groups(students: &Vec<Student>, classes: &HashMap<i64, Class>) -> () {
    todo!()
}
